# Vehicle Dashboard

Local vehicle dashboard workspace built with Vite, React, TypeScript, and pnpm.

## Run Locally

Install dependencies:

```bash
pnpm install
```

Start the dashboard:

```bash
pnpm dev
```

Open:

```text
http://localhost:5173
```

## Checks

Run typecheck and production builds:

```bash
pnpm build
```
